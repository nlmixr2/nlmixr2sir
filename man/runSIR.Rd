% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sir-run.R
\name{runSIR}
\alias{runSIR}
\title{Run sampling importance resampling for an nlmixr2 fit}
\usage{
runSIR(
  fit,
  nSamples = c(1000, 1000, 1000, 2000, 2000),
  nResample = c(200, 400, 500, 1000, 1000),
  directory = NULL,
  fitName = NULL,
  control = runSIRControl(),
  ...
)
}
\arguments{
\item{fit}{An nlmixr2 fit object with a covariance matrix.}

\item{nSamples}{Integer vector. Requested number of samples per iteration.}

\item{nResample}{Integer vector. Requested number of resamples per
iteration. Must have the same length as \code{nSamples}.}

\item{directory}{Output directory. Run artifacts, saved state and
diagnostics are written here. When \code{NULL} (default), \strong{nothing is written
to disk}: supplying a directory is what grants permission to write, so a
run that is not given one leaves the filesystem untouched. Recovery and
\code{addIterations} read the saved state, so both need a directory.}

\item{fitName}{Optional fit label used in canonical raw-results metadata and
in the names of the files written under \code{directory}. When \code{NULL} (default), the
label is derived from the expression supplied to \code{fit}.}

\item{control}{A \code{\link[=runSIRControl]{runSIRControl()}} object holding everything that tunes
how the run behaves: inflation, caps, recentering, Box-Cox, parallelism,
resume behaviour, and the OMEGA fallback.}

\item{...}{Reserved for future PsN-compatible inputs. Passing a run
setting here is an error; put it in \code{control} instead.}
}
\value{
A data frame of final SIR summary statistics with class
\code{c("nlmixr2SIR", "data.frame")}. Attributes include
\code{iterationSummary}, \code{iterations}, \code{resampledMat}, \code{covMatrix},
\code{corMatrix}, and \code{outputDir}.
}
\description{
\code{runSIR()} runs one or more sampling importance resampling iterations using
an nlmixr2 fit as the reference model. It follows the PsN SIR workflow where
practical for nlmixr2: sample parameter vectors from a proposal covariance,
evaluate them with population parameters fixed, compute importance weights,
resample, and use the empirical resampled covariance as the next proposal.
}
\examples{
\dontrun{
sir <- runSIR(
  fit,
  nSamples = c(1000, 1000, 1000),
  nResample = c(200, 400, 500),
  control = runSIRControl(workers = 4, rxThreads = 2)
)
}
}
\seealso{
\code{\link[=runSIRControl]{runSIRControl()}} for the run settings.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sir-control.R
\name{runSIRControl}
\alias{runSIRControl}
\title{Configure an \code{nlmixr2sir} run}
\usage{
runSIRControl(
  thetaInflation = 1,
  omegaInflation = 1,
  sigmaInflation = 1,
  capCorrelation = 0.8,
  capResampling = 1,
  recenter = TRUE,
  boxcox = TRUE,
  workers = NULL,
  rxThreads = NULL,
  recover = TRUE,
  addIterations = FALSE,
  saveFiles = TRUE,
  objfTolerance = 0.01,
  objfStencil = TRUE,
  objfStencilTolerance = 1,
  omegaFallback = c("cov", "wishart"),
  sigmaFallbackRse = 30,
  omegaDf = NULL,
  rseTheta = NULL,
  rseOmega = NULL,
  rseSigma = NULL,
  covmatInput = NULL,
  rawresInput = NULL,
  offsetRawres = 1L,
  inFilter = NULL
)
}
\arguments{
\item{thetaInflation, omegaInflation, sigmaInflation}{Non-negative variance
multipliers applied to the THETA, OMEGA, and residual-error blocks of the
\strong{initial} proposal. From the second iteration onwards the proposal is
the previous iteration's empirical covariance and is not re-inflated.}

\item{capCorrelation}{Numeric in \verb{[0, 1]}. Maximum absolute proposal
correlation after covariance construction and updates.}

\item{capResampling}{Numeric greater than or equal to one. \code{1} resamples
without replacement; larger values allow limited replacement.}

\item{recenter}{Logical. If \code{TRUE}, recenter the next proposal on the best
sampled vector when any sample has negative dOFV.}

\item{boxcox}{Logical. If \code{TRUE}, use Box-Cox transformed empirical
covariances for non-final iterations.}

\item{workers}{\code{NULL}, \code{"auto"}, \code{1}, or a positive integer. Controls
parallel OFV evaluation through \code{future}. \code{NULL} leaves the current
\code{future::plan()} unchanged, \code{1} forces sequential execution, a positive
integer temporarily uses a multisession plan, and \code{"auto"} uses
\code{future::availableCores(omit = 1L)}. SIR iterations are always sequential,
because each iteration builds the next iteration's proposal.}

\item{rxThreads}{Integer, \code{"auto"}, or \code{NULL}; rxode2 OpenMP threads per
worker. \code{NULL} (the default) uses the current \code{rxode2::getRxThreads()}
value for every worker; \code{"auto"} divides the core count evenly across
workers. Whenever \code{workers > 1}, \code{workers * rxThreads} must not exceed the
machine's core count, since each worker is a separate process running its
own rxode2 thread pool.}

\item{recover}{Logical. If \code{TRUE} and the output directory holds
\code{sir_state.rds}, resume from the last completed iteration when possible.}

\item{addIterations}{Logical. If \code{TRUE}, append the supplied schedule after
an existing completed state in the output directory.}

\item{saveFiles}{Logical. If \code{TRUE} (default) the run writes its raw
results, summaries, covariance files, seed and resumable state to
\code{directory}. Set to \code{FALSE} to run entirely in memory, writing nothing and
creating no directory; the result is returned as usual. Recovery,
\code{addIterations}, and per-iteration seed reproduction all need the saved
state, so they are unavailable when this is \code{FALSE} -- seed a run with
\code{set.seed()} beforehand to reproduce it.}

\item{objfTolerance}{Non-negative number, default \code{1e-2}. Before sampling,
SIR re-evaluates the objective at the fit's own estimates and compares it
with \code{fit$objf}; the run aborts unless the absolute difference is within
this tolerance, and warns above \code{1e-3}. It is the per-run evidence that
candidates are scored on the same surface the dOFVs are measured against.

The default is set from what a gap does rather than from how big it
"should" be: a dOFV error of \code{d} moves an importance weight by
\code{exp(-d/2)}, so \code{1e-2} costs under 0.5\% against dOFV of order 1 to 10,
while still catching a genuinely different surface by more than two orders
of magnitude. Ordinary reproduction gaps are inner-solve convergence
slack, which only \code{sigdig} predicts; refitting at a higher \code{sigdig}
shrinks a gap roughly 3-4 fold per digit, and is the remedy to prefer over
raising this.}

\item{objfStencil}{Logical. If \code{TRUE} (default) the objective preflight also
evaluates small perturbations either side of every parameter and requires
the fitted estimates to remain a local optimum. Agreement at the centre
alone does not establish that two objectives are the same function.}

\item{objfStencilTolerance}{Non-negative number. How much a probe may lower
the objective before the run is refused. Smaller decreases warn instead,
since a fit that stopped just short of convergence is common and harmless.}

\item{omegaFallback}{How OMEGA uncertainty is obtained. \code{"cov"} (the
default) takes it from \code{fit$cov}, including its correlations with THETA,
and degrades to \code{"wishart"} automatically when \code{fit$cov} does not carry
OMEGA. \code{"wishart"} always uses the Wishart-style approximation, which
gives a block-diagonal proposal.}

\item{sigmaFallbackRse}{Percent relative standard error used for
residual-error uncertainty when neither \code{fit$cov} nor \code{fit$parFixedDf}
reports a standard error.}

\item{omegaDf}{Optional degrees of freedom for the Wishart-style OMEGA
fallback; defaults to \code{nsub - 1}. Unused when OMEGA comes from \code{fit$cov}.}

\item{rseTheta, rseOmega, rseSigma}{Relative standard errors, as percentages,
used to build a diagonal proposal without a covariance step. Each is
either a single value for the whole class or one value per estimated
(non-fixed) element of it -- THETA, OMEGA \strong{diagonals}, residual error.
Diagonal variances come out as \code{(rse * estimate / 100)^2}; OMEGA
off-diagonals are derived from the two diagonals they connect. Following
PsN, a scalar \code{rseTheta} fills in an unset \code{rseOmega} and \code{rseSigma}, a
vector \code{rseTheta} does not, and setting \code{rseOmega} without \code{rseTheta} is
an error. Cannot be combined with \code{covmatInput} or with inflation.}

\item{covmatInput}{A proposal covariance supplied directly: a numeric
matrix, a path to a NONMEM-style \code{.cov} file, or the string \code{"identity"}.
An unnamed matrix must cover the whole SIR parameter vector in order; a
named one may be a subset, keyed by either SIR or \code{fit$cov} names.
\code{"identity"} together with inflation is the cheap "any diagonal proposal"
route. Cannot be combined with \code{rseTheta}.}

\item{rawresInput}{A canonical raw-results file path or data frame whose
parameter vectors seed the first proposal, as PsN's iteration 0 does: the
empirical mean and covariance of the supplied vectors become the
iteration-1 proposal. Any canonical raw-results file works, including one
written by \code{nlmixr2boot}. Cannot be combined with \code{rseTheta} or
\code{covmatInput}.}

\item{offsetRawres}{Integer. Skip raw-results samples numbered below this.
Defaults to \code{1}, which drops the reference row.}

\item{inFilter}{Optional filter applied to the raw-results rows before they
are used, in any form accepted by
\code{\link[nlmixr2utils:setupRawResultsFilter]{nlmixr2utils::setupRawResultsFilter()}}. Only meaningful with
\code{rawresInput}.}
}
\value{
An object of class \code{runSIRControl}.
}
\description{
\code{runSIRControl()} constructs and validates the control object used by
\code{\link[=runSIR]{runSIR()}}. It holds everything that tunes \emph{how} SIR runs, leaving
\code{\link[=runSIR]{runSIR()}} itself to take only the fit, the sampling schedule, and where
output goes.
}
\examples{
runSIRControl(thetaInflation = 2, workers = 4, rxThreads = 2)
}
